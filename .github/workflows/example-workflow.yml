name: Example workflow

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:  
      - uses: actions/checkout@v3
      - name: build
        run: docker build -t test -f dockerfile .
      - name: test
        run: docker run -i --rm test