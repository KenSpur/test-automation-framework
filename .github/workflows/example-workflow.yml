name: Example workflow

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:  
      - uses: actions/checkout@v3
      - name: build
        run: docker build -t test -f dockerfile .
      - name: test
        run: docker run -v "$RUNNER_TEMP:/mnt/out" -i --rm test --filter "Category=local | Category=internet & Category!=intranet"
      - name: display $RUNNER_TEMP content
        run: ls -a $RUNNER_TEMP